# Python / venv
.venv/
__pycache__/
*.pyc
build/
dist/
*.spec
*.egg-info/

# Generated assets (recreated by install.py)
app_icon.png
app.ico

# User/runtime data — usage history can be sensitive, never commit it
config.json
state.json
history.json
last_snapshot.json
*.log

# Local operational record (personal notes — not for the public repo)
docs/HANDOFF.md

# Tooling scratch
.playwright-mcp/

# Screenshot scratch at repo root (curated copies live in docs/)
/dash-*.png
/widget-*.png
/icon-montage.png
